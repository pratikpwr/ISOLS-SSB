name: how_to_crack_ssb
description: How to Crack SSB - ISOLS Group

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  webview_flutter: ^1.0.7
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_native_splash: ^0.1.9


flutter:

  uses-material-design: true

  assets:
    - assets/

flutter_native_splash:
  image: assets/logo_hd_50.png
  color: "ffffff"